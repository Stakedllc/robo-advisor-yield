module.exports = async function (deployer, network, accounts) {

  console.log(`Deploying to ${network}`);

  if (network == 'development') {

    // can migrate your own smart contracts here if desired

  }

};
